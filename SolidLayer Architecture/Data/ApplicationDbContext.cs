// This file is no longer needed and can be deleted or commented out

/*
using Microsoft.EntityFrameworkCore;
using Swipe2TryCore.Models;

namespace SolidLayer_Architecture.Data
{
    // We're not using Entity Framework anymore, so this class is obsolete
    public class ApplicationDbContext : DbContext
    {
        public ApplicationDbContext(DbContextOptions<ApplicationDbContext> options) : base(options) { }

        // DbSet properties removed as we're using raw SQL now
    }
}
*/
